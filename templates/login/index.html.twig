{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">{{ 'page_title'|trans }}</div>
                    <div class="card-body">
                        <a href="{{ path('connect_google') }}" class="btn btn-primary">{{ 'google_connexion'|trans }}</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}